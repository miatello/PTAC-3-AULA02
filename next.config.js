/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "www.ifms.edu.br",
            "www.google.com/"

        ]
    }
}

module.exports = nextConfig
